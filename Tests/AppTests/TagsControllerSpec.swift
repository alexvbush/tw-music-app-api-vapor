import XCTest
import Testing
import Quick
import Nimble

class TagsControllerSpec: QuickSpec {
    override func spec() {
        describe("something") {
            it("does somehting") {
                expect(true).toNot(beTrue())
            }
        }
    }
}
